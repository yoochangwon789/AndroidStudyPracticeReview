<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidstudypractice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidStudyPractice">
        <activity android:name=".RecTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TabLayoutPagerActivity"></activity>
        <activity android:name=".RecyclerViewBook">

        </activity>
        <activity android:name=".ListViewActivity" />
        <activity android:name=".PhoneBookSelect" />
        <activity android:name=".PhoneBookActivity" />
        <activity android:name=".AddView" />
        <activity android:name=".FragmentActivity" />
        <activity android:name=".Intent2" />
        <activity android:name=".Intent1" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>